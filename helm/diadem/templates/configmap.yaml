apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "diadem.fullname" . }}-config
  labels:
    {{- include "diadem.labels" . | nindent 4 }}
data:
  config.toml: |
    {{- .Values.config | nindent 4 }}

    {{- include "diadem.dbConfig" . | nindent 4 }}
